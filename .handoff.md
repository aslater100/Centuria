# Session Handoff

**Session date:** 2026-06-27 (pm) · **Repo:** aslater100/Centuria · **Tests:** 939 passing · tsc clean · vite build green · headless 8-seed × 181y byte-identical to pre-change

> Full persistent guide is **`HANDOFF.md`** — its top status block + the new
> **"Prioritized non-asset backlog"** section are where the next session picks up.
> This file is the short per-session baton. Read both.

---

## 1. TARGET BRANCH

`claude/sharp-bell-yb4f7f` — the designated working branch (do not push elsewhere
without explicit permission). Off `origin/main` (which carries #283, the cost-push
PR, merged). **This session's work is PR #284 (open, 4 commits).**

## 2. COMPLETED (this session — PR #284, 4 commits)

Brief: "finish the rest of the non-asset work." Ran an 8-reader + synthesizer
**inventory workflow** to ground-truth the roadmap, then shipped the safest,
highest-value byte-identical items:

1. **D1-econ trade leg** — supply shock chokes exports
   (`exportEarningsLastMonth ×= 1 − severity·SUPPLY_SHOCK_EXPORT_DRAG`, 0.5). With
   the merged GDP drag + cost-push, all three legs (GDP, prices, trade) are wired.
   `tests/supply-trade-leg.test.ts`. Byte-identical (severity 0 in healthy play).
2. **Determinism + save-fidelity hardening (Track C guard).** New full-state
   `serialize()` harness (`tests/serialize-determinism.test.ts`) caught + fixed:
   (a) **5 `Math.random()` sites** → non-reproducible saves; notable health + loan
   ids now use a serialized **`auxRng`** (mirrors `aiRng`; main+AI streams stay
   byte-identical). (b) `supplyShockMult`/`_electronicsDisrupted` (one-month-lagged
   caches) weren't serialized → mid-shock reload dropped the drag; now persisted.
   (c) Phase-14 fields + notable defaults weren't round-trip-symmetric → shared
   `cityServiceFields()` helper + spread-first notable backfill make saves lossless.
3. **First C1 extraction** — `tickPollution` → `src/sim/systems/pollution.ts`
   (`fn(r: RegionSim)`, the roadmap pattern), proven byte-identical by the harness.
4. **Track D** — `bench-region` re-baselined to the wall-clock frame model; gate is
   now the worst single tick (>16.7 ms = stutter; currently ~10.6 ms, passes).

## 3. IN PROGRESS

- **Nothing half-done.** Branch green + complete. PR #284 open (draft → ready is the
  user's call). CI = build + vitest; all verified locally.

## 4. FAILED PATHS / GOTCHAS (save the next agent time)

- **`auxRng` is a THIRD serialized RNG**, deliberately separate from `rng`/`aiRng`
  so the previously-`Math.random` draws (notable health, loan ids) became
  deterministic WITHOUT shifting the main/AI streams → every existing seed outcome
  is byte-identical (headless 8-seed unchanged). Any new "incidental" stochastic
  detail that must not perturb gameplay belongs on `auxRng`, not `rng`.
- **`advisorForecast` keeps its `Math.random()` on purpose** — it's UI-only display
  noise (3 callers, all in `regionview.ts`), never mutates serialized state; seeding
  it from a serialized RNG would let UI re-renders corrupt save determinism.
- **The determinism harness compares FULL `serialize()`**, not 8 scalars — that's
  why it caught what `region-longrun`'s snapshot didn't. Its reload tests compare
  **canonical (post-load) forms** so benign migration-backfill differences don't mask
  real tick-state loss (that canonical compare is what caught the supply caches).
- **C1 extraction recipe (proven):** move the body verbatim (`this.`→`r.`) into
  `systems/<name>.ts` as `export function tick<Name>(r: RegionSim)`, `import type
  { RegionSim }` (no runtime cycle), have `tick()` call the free function, update any
  tests that cast-and-called the private method to call the free function. Verify
  with the determinism harness (byte-identical) + full suite. Needs every touched
  `r.x` to be PUBLIC — `tickServiceCoverage` (next) qualifies; check before others.
- **`npm install`:** `ELECTRON_SKIP_BINARY_DOWNLOAD=1 PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
  npm install --ignore-scripts --no-audit --no-fund`.
- **Asset/audio generation** still network-blocked (`huggingface.co` 403, no
  sharp/ffmpeg). Not this session's scope ("not the ai assets yet").

## 5. NEXT STEPS (immediate — see HANDOFF.md "Prioritized non-asset backlog")

1. **C1: extract `tickServiceCoverage`** → `systems/services.ts` (same recipe as
   pollution, now de-risked by the harness). Keep extracting leaves before piling
   new econ features onto the 14k-line monolith.
2. **D3-ai: situation-aware `DealVerdict`** — `rivalSituation(rv)∈[0,1]` from data
   already on the rival; scale pact/trade appetite, gated `sit>0` → byte-identical.
   High gameplay value, no re-baseline.
3. **D3-ai: tier-asymmetry guardrail** + **structured `AgendaKind`** (both TRUE/safe).

Then Tier-2 (own PRs, re-baseline expected): FX-writer consolidation, revanchism CB,
climate-refugee migration. Tier-3 (large): spatial military Front, physical-goods/
per-good-prices/44-good econ, zoning grid maps. Assets (`B1-art`/`B2-audio`) need an
env with network egress + image/audio tooling.

**Open from #283:** the Phase-2 graded extraction proxy is still an
unverified-by-human balance change; the new trade leg + cost-push ride the same
severity signal, so a downturn playtest now exercises all three legs at once.
